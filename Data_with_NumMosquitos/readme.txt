first run make_aggregated_with_nummopsquitos
then run predit_mosquitos with your favourite regressor (right now the values for 'Date','Address','Species','Street','AddressNumberAndStreet','Trap' are dropped because they contain strings. We should make dummies out of them)
